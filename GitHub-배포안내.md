# GitHub Pages로 배포하기 — 단계별 안내

오토마타 모듈 시뮬레이터를 학생들에게 링크 하나로 나눠 주기 위한 안내서입니다.
Git이나 명령어는 전혀 쓰지 않고, GitHub 웹사이트에서 마우스만으로 끝냅니다. 처음 해도 10분 안쪽입니다.

준비물은 `index.html` 파일 하나와 GitHub 계정입니다.

---

## 왜 Apps Script에서 안 열렸나

GitHub로 옮기기 전에, 같은 문제를 다시 겪지 않도록 원인을 정리해 둡니다.
`Code.gs`와 `index.html` 자체에는 잘못이 없었고, 아래 다섯 가지가 흔한 원인입니다.

**1. HTML 파일 이름이 어긋났을 가능성이 가장 큽니다.** `HtmlService.createHtmlOutputFromFile('index')`는
프로젝트 안에 정확히 `index`라는 이름의 HTML 파일을 찾습니다. 그런데 Apps Script 편집기에서 파일을 만들 때
이름 칸에 `index.html`이라고 적으면 실제 파일명은 `index.html.html`이 되어 버립니다.
그러면 "파일을 찾을 수 없습니다" 오류가 나면서 화면이 아예 안 열립니다. 이름 칸에는 `index`만 적어야 합니다.

**2. 코드를 고친 뒤 새 배포를 만들지 않으면 예전 버전이 계속 보입니다.** `/exec` 주소는 "배포된 버전"을
가리키기 때문에, 편집기에서 저장만 해서는 반영되지 않습니다. 배포 → 배포 관리 → 연필 → 버전을 "새 버전"으로
바꿔야 합니다. 편집 중인 코드를 바로 보려면 `/dev` 주소를 쓰는데, 이 주소는 본인만 열 수 있어서 학생에게 주면 안 됩니다.

**3. 액세스 권한이 "나만"으로 되어 있으면 학생은 권한 요청 화면만 봅니다.** 배포 설정에서 액세스 권한을
학교 도메인 사용자 또는 Google 계정이 있는 모든 사용자로 바꿔야 합니다.

**4. 학교망이나 교육청망이 Apps Script 웹앱을 막는 경우가 많습니다.** Apps Script 웹앱의 화면은
`script.google.com`이 아니라 `*.googleusercontent.com`이라는 별도 주소에서 그려지는데, 학교 네트워크나
관리형 크롬북 정책이 이 주소를 차단하면 "접속이 안 됩니다" 증상이 그대로 나타납니다. 선생님 계정에서는
되는데 학생 계정에서만 안 되는 경우가 대표적입니다. 이건 코드로 해결할 수 없습니다.

**5. 학생 계정이 관리자 정책으로 서드파티 앱 실행을 못 하게 막혀 있으면 로그인 단계에서 끊깁니다.**

GitHub Pages는 로그인도, 권한 승인도, 구글 계정도 필요 없는 평범한 웹페이지로 서비스되기 때문에
3~5번 문제가 원리적으로 생기지 않습니다. 이게 옮기는 가장 큰 이유입니다.

---

## 1단계 — GitHub 계정 만들기

이미 계정이 있으면 2단계로 넘어가세요.

1. https://github.com/signup 접속
2. 이메일, 비밀번호, 사용자 이름(username)을 입력합니다.
   **이 사용자 이름이 그대로 주소에 들어갑니다.** 학생에게 보여줄 주소가 되니 `kim-tech`, `hong-eng`처럼
   짧고 알아보기 쉬운 영문으로 정하세요. 나중에 바꾸면 주소도 바뀌어 버립니다.
3. 이메일 인증까지 마칩니다. 무료 계정으로 충분합니다.

---

## 2단계 — 저장소(repository) 만들기

1. 오른쪽 위 **+** → **New repository**
2. 항목을 이렇게 채웁니다.
   - **Repository name**: `automata-sim` (이 이름도 주소에 들어갑니다. 영문 소문자와 하이픈만 쓰세요)
   - **Description**: 비워 둬도 됩니다
   - **Public** 선택 — 반드시 Public이어야 합니다. Private으로 하면 무료 계정에서는 Pages가 동작하지 않습니다
   - 아래 체크박스(Add a README 등)는 모두 **끄세요**
3. **Create repository** 클릭

만든 직후 나오는 빈 화면에 "uploading an existing file"이라는 링크가 보이는데, 다음 단계에서 씁니다.

---

## 3단계 — 파일 올리기

1. 방금 화면의 **uploading an existing file** 링크를 클릭합니다.
   (이미 화면을 벗어났다면 저장소에서 **Add file** → **Upload files**)
2. `index.html`과 `README.md`를 점선 상자 안으로 끌어다 놓습니다.
   `README.md`는 없어도 사이트는 정상 동작하니, 귀찮으면 `index.html`만 올려도 됩니다.
3. 아래 **Commit changes** 버튼을 누릅니다.

> 파일 이름은 반드시 소문자 `index.html`이어야 합니다. `Index.html`이나 `index .html`처럼
> 대문자나 공백이 섞이면 주소를 열었을 때 404가 납니다.

---

## 4단계 — GitHub Pages 켜기

1. 저장소 위쪽 메뉴에서 **Settings**(톱니바퀴) 클릭
2. 왼쪽 사이드바에서 **Pages** 클릭
3. **Build and deployment** 항목을 이렇게 맞춥니다.
   - **Source**: `Deploy from a branch`
   - **Branch**: `main` / 폴더는 `/ (root)`
4. **Save** 클릭

1~2분 뒤 같은 화면 위쪽에 초록색으로 주소가 나타납니다.

```
https://사용자이름.github.io/automata-sim/
```

바로 안 뜨면 페이지를 새로 고쳐 보세요. 첫 배포는 최대 3분 정도 걸립니다.
저장소 메인 화면의 **Actions** 탭에서 초록 체크 표시가 뜨면 완료된 것입니다.

---

## 5단계 — 확인하고 학생에게 나눠 주기

발급된 주소를 **시크릿 창**(`Ctrl`+`Shift`+`N`)에서 한 번 열어 보세요.
로그인 없이 바로 시뮬레이터가 돌아가면 성공입니다. 이게 학생이 보게 될 화면과 같습니다.

가능하면 학교 학생용 계정이나 실습실 컴퓨터에서 한 번 더 열어 확인하세요.
Apps Script 때와 달리 차단될 이유는 거의 없지만, 실습 당일에 처음 확인하는 것보다 안전합니다.

나눠 줄 때는 이렇게 하면 편합니다.

- 구글 클래스룸 공지에 링크를 걸어 둡니다
- 칠판에 QR 코드를 띄웁니다. 크롬 주소창 오른쪽 끝의 공유 아이콘 → QR 코드 만들기로 즉석에서 만들 수 있습니다
- 차시별로 `?ex=3`, `?ex=6`처럼 예제 번호를 붙인 링크를 따로 나눠 주면 학생이 헤매지 않습니다 (번호표는 `README.md`에 있습니다)

---

## 나중에 고치고 싶을 때

1. 저장소에서 `index.html` 클릭
2. 오른쪽 위 연필 아이콘(Edit this file) 클릭
3. 고친 뒤 아래 **Commit changes** 클릭
4. 1~2분 뒤 사이트에 반영됩니다

화면이 그대로 보이면 브라우저가 예전 파일을 기억하고 있는 것이니 `Ctrl`+`Shift`+`R`로 강제 새로 고침하세요.
학생들에게도 "안 바뀌면 Ctrl+Shift+R" 한 줄만 알려 주면 됩니다.

Apps Script처럼 "새 배포를 만들어야 반영되는" 단계가 없다는 점이 큰 차이입니다. 고치면 바로 그게 배포본입니다.

---

## 알아 둘 점 두 가지

**저장 기능의 범위가 달라집니다.** GitHub Pages에는 서버가 없으므로 저장/불러오기가 그 컴퓨터의 그 브라우저
안에서만 동작합니다(브라우저 저장소 사용). 실습실 공용 컴퓨터에서 다른 자리에 앉으면 자기 설계가 안 보이고,
브라우저 기록을 지우면 사라집니다. 그래서 **제출과 이어서 작업은 JSON 버튼**으로 하도록 안내해 주세요.
JSON 텍스트를 복사해 클래스룸이나 설문지에 붙이면 되고, 선생님은 그 텍스트를 JSON 창에 붙여 넣고
적용을 누르면 학생 작품이 그대로 열립니다. 채점할 때 오히려 편합니다.

**주소가 공개됩니다.** Public 저장소이므로 주소를 아는 사람은 누구나 열 수 있고, 코드도 볼 수 있습니다.
수업용 시뮬레이터라면 문제될 것이 없지만, 학생 개인정보나 평가 자료는 이 저장소에 절대 올리지 마세요.

---

## 잘 안 될 때

**주소를 열었는데 404가 나옵니다.** 파일 이름이 정확히 `index.html`인지(대소문자 포함), 저장소 최상위에
있는지(하위 폴더 안에 들어가면 안 됩니다) 확인하세요. Settings → Pages에서 Branch가 `main`, 폴더가 `/ (root)`인지도
같이 보세요. 방금 켰다면 3분만 더 기다려 보세요.

**Settings에 Pages 메뉴가 없습니다.** 저장소가 Private일 가능성이 큽니다.
Settings 맨 아래 Danger Zone에서 **Change visibility** → Public으로 바꾸세요.

**화면은 열리는데 글꼴이 다릅니다.** 글꼴만 인터넷에서 불러오는데 그 요청이 차단된 경우입니다.
시스템 글꼴로 자동 대체되고 기능에는 아무 영향이 없으니 그냥 쓰셔도 됩니다.

**모듈이 안 움직이거나 화면이 하얗게 나옵니다.** 파일 업로드가 중간에 잘린 경우입니다.
저장소에서 `index.html`을 클릭해 마지막 줄이 `</html>`로 끝나는지 확인하고, 아니면 다시 올리세요.
